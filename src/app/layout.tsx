import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    default: "Sacred Cipher Mission | Faith. Purpose. Impact.",
    template: "%s | Sacred Cipher Mission",
  },
  description:
    "Sacred Cipher Mission exists to uplift communities, unlock human potential, and create sustainable change through education, innovation, and compassion.",
  keywords: ["nonprofit", "charity", "education", "community", "impact", "mission"],
  openGraph: {
    type: "website",
    locale: "en_US",
    siteName: "Sacred Cipher Mission",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
